{
  "extends": "./tsconfig.base.json",
  "include": ["scripts", "oxfmt.config.ts"],
  "compilerOptions": {
    "customConditions": [],
    // Base config sets `types: []`; these scripts run on node
    "types": ["node"]
  }
}
